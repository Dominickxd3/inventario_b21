# =============================================================================
# Plantilla .env (copiar a .env y rellenar)
# =============================================================================

# --- SQL Server ---
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=BD_Inventario_B21
DB_USER=sa
DB_PASSWORD=tu_clave_aqui
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# --- JWT ---
JWT_SECRET=genera_una_clave_aleatoria_de_al_menos_32_caracteres
JWT_EXPIRES_IN=8h

# --- API ---
PORT=3000

# --- Google Drive (Fase 5) ---
# Ruta del archivo JSON de la service account (Google Cloud Console -> IAM -> Service Accounts -> Keys)
GOOGLE_DRIVE_SERVICE_ACCOUNT=
# REAL exige credenciales configuradas; vacio = modo simulado (no sube nada, genera IDs fake)
DRIVE_MODO=SIMULADO
